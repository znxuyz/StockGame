# Supabase 雲端同步用環境變數。
#
# 本機開發:複製這個檔案成 .env.local 後填入 Supabase Project 的值。
#   cp .env.example .env.local
#
# Cloudflare Pages 部署:在 Pages dashboard → Settings → Environment variables
# 加入相同名稱的變數。Production / Preview 兩個 environment 都要加。
#
# 兩個值在 Supabase dashboard → Project Settings → API 找:
#   - Project URL              → VITE_SUPABASE_URL
#   - anon / public key        → VITE_SUPABASE_ANON_KEY
#
# 不要把 service_role key 放這裡。那把要放 server-only 環境變數
# (Cloudflare Function 用),Phase D 才會用。

VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key-here

# 第三方登入按鈕 feature flag(預設不顯示)。
# 在 Supabase Dashboard 啟用對應 provider + 拿到 Client ID/Secret 之後再開,
# 不然按鈕點下去會顯示「provider is not enabled」錯誤。
# 任意非 'true' 值都當 false。
# VITE_ENABLE_APPLE_LOGIN=true
# VITE_ENABLE_GOOGLE_LOGIN=true
